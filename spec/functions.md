# Functions

Uncategorized:

- Optional parameters
- Rest parameter
- `where` clause for generic functions

Getters/setters:

```
function get x():Number 10;
function set x(value:Number) {
}
```

Constructor:

```
class C {
    function C() {
    }
}
```